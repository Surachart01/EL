version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: el-mongodb
    ports:
      - "27017:27017"
    volumes:
      - el-mongodb-data:/data/db
    restart: unless-stopped

volumes:
  el-mongodb-data:
